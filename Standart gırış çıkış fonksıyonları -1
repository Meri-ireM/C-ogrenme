
#define PI 3.14
//Bu kütüphaneye pi sayısı tanımlamak istiyorum.
#include <stdio.h>

int main() {
	
	int yaricap; // "kullanıcıdan yarıçap istiyorum"
	float hacim; // bu ikisi tanımlama
	
	printf("kurenin yaricapini giriniz : ");
	scanf("%d",&yaricap); //değer almak istiyorum
	
	hacim = (4/3.0)*PI*(yaricap*yaricap*yaricap);
	// 3.0 ın nedeni float veri istiyorum. biri float olunca gelecek
	
	printf("Kurenin hacmi %.3f `dir", hacim);
	// %f nin yerine hesapladığımız hacmi koyacak+ .3 --> virgülden sonra 3 basmk.
	
	return 0;
}

sonuç:

kurenin yaricapini giriniz : 8
Kurenin hacmi 2143.573 `dir
--------------------------------
Process exited after 1.774 seconds with return value 0
Press any key to continue . . .
